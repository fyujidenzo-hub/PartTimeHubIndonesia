import { Trash2, X } from 'lucide-react'
import { useEffect } from 'react'

export function ConfirmDialog({ open, title, description, loading, onConfirm, onClose }: { open: boolean; title: string; description: string; loading?: boolean; onConfirm: () => void; onClose: () => void }) {
  useEffect(() => { const listener = (event: KeyboardEvent) => { if (event.key === 'Escape') onClose() }; if (open) window.addEventListener('keydown', listener); return () => window.removeEventListener('keydown', listener) }, [open, onClose])
  if (!open) return null
  return <div className="fixed inset-0 z-[70] grid place-items-center p-4" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title"><button aria-label="Tutup dialog" onClick={onClose} className="absolute inset-0 bg-slate-950/45 backdrop-blur-[2px]" /><div className="animate-fade-up relative w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><button onClick={onClose} className="absolute right-4 top-4 grid size-8 place-items-center rounded-lg text-slate-400 hover:bg-slate-50"><X className="size-4" /></button><div className="grid size-11 place-items-center rounded-xl bg-red-50 text-red-600"><Trash2 className="size-5" /></div><h2 id="confirm-title" className="mt-4 text-lg font-black text-slate-900">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{description}</p><div className="mt-6 flex justify-end gap-2"><button disabled={loading} onClick={onClose} className="rounded-xl border border-slate-200 px-4 py-2.5 text-xs font-extrabold text-slate-600 hover:bg-slate-50">Batal</button><button disabled={loading} onClick={onConfirm} className="rounded-xl bg-red-600 px-4 py-2.5 text-xs font-extrabold text-white hover:bg-red-700 disabled:opacity-60">{loading ? 'Menghapus…' : 'Hapus Lowongan'}</button></div></div></div>
}
